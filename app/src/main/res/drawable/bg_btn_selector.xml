<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" android:enterFadeDuration="500" android:exitFadeDuration="500">
    <!-- 不可用时 -->
    <item android:drawable="@drawable/bg_oval_with_stroke_only" android:state_enabled="false" />
    <!-- 按压时 -->
    <item android:drawable="@drawable/bg_oval_with_size" android:state_pressed="true" />
    <!-- 默认时 -->
    <item android:drawable="@drawable/bg_oval_with_stroke_only" />
</selector>